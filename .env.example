# =============================================================================
# Data Lakehouse — Variáveis de Ambiente
# Copie para .env e ajuste os valores: cp .env.example .env
# =============================================================================

# --- PostgreSQL (Fonte OLTP) -------------------------------------------------
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=datalakehouse
POSTGRES_USER=lakehouse
POSTGRES_PASSWORD=changeme_pg

# --- Kafka (KRaft) -----------------------------------------------------------
KAFKA_BROKER=kafka:9092
KAFKA_CLUSTER_ID=MkU3OEVBNTcwNTJENDM2Qk

# --- MinIO (S3-compatible) ---------------------------------------------------
MINIO_ENDPOINT=http://minio:9000
MINIO_ROOT_USER=lakehouse
MINIO_ROOT_PASSWORD=changeme_minio
MINIO_BUCKET_BRONZE=bronze
MINIO_BUCKET_SILVER=silver
MINIO_BUCKET_GOLD=gold

# --- Iceberg REST Catalog ----------------------------------------------------
CATALOG_URI=http://iceberg-rest:8181
CATALOG_WAREHOUSE=s3://warehouse/

# --- Airflow -----------------------------------------------------------------
AIRFLOW__CORE__FERNET_KEY=changeme_fernet_key
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
AIRFLOW_UID=50000

# --- DuckDB / MotherDuck -----------------------------------------------------
MOTHERDUCK_TOKEN=

# --- FastAPI -----------------------------------------------------------------
API_HOST=0.0.0.0
API_PORT=8000
API_SECRET_KEY=changeme_api_secret

# --- Debezium / Kafka Connect ------------------------------------------------
CONNECT_BOOTSTRAP_SERVERS=kafka:9092
CONNECT_GROUP_ID=lakehouse-connect
CONNECT_REST_PORT=8083

# =============================================================================
# Data Lakehouse — Dependências Python
# =============================================================================

# --- Core / Ingestão ---------------------------------------------------------
confluent-kafka>=2.6.0
pyarrow>=18.0.0
pyiceberg>=0.8.0

# --- Storage (S3/MinIO) ------------------------------------------------------
boto3>=1.35.0
minio>=7.2.0

# --- Processamento / Analytics ------------------------------------------------
duckdb>=1.2.0

# --- Transformação (dbt) -----------------------------------------------------
dbt-core>=1.9.0
dbt-duckdb>=1.9.0

# --- Orquestração -------------------------------------------------------------
apache-airflow>=2.10.0

# --- API / Serving ------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pyjwt>=2.9.0

# --- IA / RAG ----------------------------------------------------------------
langchain>=0.3.0
chromadb>=0.5.0

# --- Qualidade / Testes -------------------------------------------------------
pytest>=8.3.0
great-expectations>=1.2.0

# --- Utilitários --------------------------------------------------------------
python-dotenv>=1.0.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.28.0
rich>=13.9.0
